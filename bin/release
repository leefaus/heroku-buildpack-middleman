#!/usr/bin/env bash
# bin/release <build-dir>

# Just an empty hash, we don't do anything by default
cat <<EOF
---
default_process_types:
  web: sh cd $1/resources && bundle exec middleman server -p \$PORT
EOF
